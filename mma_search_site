# will display search results on a web page, uses a form for user input
# and displays data if input is valid on the page

from flask import Flask

app = Flask(__name__)


@app.route('/')
def home():
    return ""

if __name == '__main__':
    app.run()
